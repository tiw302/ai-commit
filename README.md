# ai-commit

A highly customizable AI-powered git commit message generator written in Go.

## Installation

1. Ensure you have Go installed.
2. Clone the repository.
3. Run `go build -o ai-commit`
4. Setup your config at `~/.config/ai-commit/config.json`

## Usage

```bash
git add .
ai-commit -m "some context"
```
